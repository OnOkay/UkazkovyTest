<Window x:Class="UkazkovyTest.View.Login"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:PW="clr-namespace:UkazkovyTest.Components"
        xmlns:local="clr-namespace:UkazkovyTest.View"
        mc:Ignorable="d"
        Title="Login" Height="233" Width="291"
        DataContext="{Binding LoginWindow}">
    <Window.Resources>
        <PW:PasswordUserControl x:Key="PasswordUserControl"/>
    </Window.Resources>
    <Grid>
        <!--Tlačítko Quit vypíná celou aplikaci | Tlačítko login Zkontroluje zda jsou údaje spravně a vpustí uživatele dál. Jinak Se zobrazí label, že se něco napsalo špatně-->
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="20*"/>
            <ColumnDefinition Width="60*"/>
            <ColumnDefinition Width="20*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="15*"/>
            <RowDefinition Height="20*"/>
            <RowDefinition Height="20*"/>
            <RowDefinition Height="25*"/>
        </Grid.RowDefinitions>
        <Label Grid.Row="0" Grid.Column="1" Content="Please enter your username and password" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <Label Grid.Row="1" Grid.Column="1" Content="Username" HorizontalAlignment="Center" VerticalAlignment="Center" Height="24"/>
        <TextBox Grid.Row="1" Grid.Column="1" Name="UsernameBox" HorizontalAlignment="Center" VerticalAlignment="Bottom" Width="150"  TextWrapping="Wrap" Text="{Binding UserNameBox, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
        <Label Grid.Row="2" Grid.Column="1" Content="Password" HorizontalAlignment="Center" VerticalAlignment="Center" Height="24"/>

        <PW:PasswordUserControl Grid.Row="2" Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Bottom" Width="150" Password="{Binding PasswordBox, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
        <Button Grid.Row="3" Grid.Column="0" Command="{Binding QuitAppCommand}" Name="QuitButton" Content="Quit" HorizontalAlignment="Center" Margin="5" Height="25"/>
        <Label Grid.Row="3" Grid.Column="1" Content="{Binding MistakeAnswer, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Center" VerticalAlignment="Top" Height="29"/>
        <Button Grid.Row="3" Grid.Column="2" Command="{Binding ShowWindowCommand}" Name="LoginButton" Content="Login" HorizontalAlignment="Center" Margin="5" Height="25"/>
    </Grid>
</Window>
